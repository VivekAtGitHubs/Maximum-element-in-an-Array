#include <stdio.h>
int main()
{
    int i,a[5],max;
    for(i=0;i<5;i++)
   {
     printf(" enter number");
     scanf("%d",&a[i]);
   }
   max=a[0];
   for(i=0;i<5;i++)
       if(a[i]>max)
       max=a[i];    
       
       printf(" max elements is%d",max);
    return 0;
}
